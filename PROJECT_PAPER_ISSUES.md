# 项目代码与论文一致性问题清单

> 范围：基于当前仓库代码、`paper` 中论文内容以及已验证的运行/代码检查结果整理。  
> 目的：区分“项目真实存在的问题”“论文与实现不一致的问题”“论文需要补强的表达与实验问题”。  
> 说明：本文档只记录问题与建议，不代表已经完成代码修复。

---

## 1. 项目代码层面的已发现问题

### 1.1 预测脚本与依赖版本不兼容

**事实：** `predict_admission.py` 中调用了：

```python
model.fit(disp=False)
```

但当前环境中的 `statsmodels==0.14.6` 的 `ARIMA.fit()` 不支持 `disp` 参数，实测会抛出：

```text
TypeError: ARIMA.fit() got an unexpected keyword argument 'disp'
```

**影响：**

- 招生趋势预测功能可能无法正常输出 ARIMA 结果。
- 论文中关于 ARIMA/XGBoost 预测模块的实验结论需要重新验证。

**建议：**

- 去掉 `disp=False`。
- 增加预测脚本的最小回归测试。
- 在论文实验部分补充真实运行版本、依赖版本和预测误差指标。

---

### 1.2 管理类接口权限校验不足

**事实：** 多个管理接口存在新增、修改、删除入口，但未在 Controller 层看到统一的管理员权限校验，例如：

- `MetaDataController`
- `CollegeController`
- `MajorController`
- `ClassController`

相比之下，用户管理接口中存在显式 token/role 判断。

**影响：**

- 如果安全配置没有在全局拦截层限制角色，普通登录用户可能访问高权限写接口。
- 论文若描述系统具备完善权限管理，需要补充实际授权机制或修正表述。

**建议：**

- 在后端增加统一鉴权注解或拦截器，例如 `@PreAuthorize("hasRole('ADMIN')")`。
- 对所有写接口做权限矩阵。
- 论文中避免笼统写“权限控制完善”，除非有可验证实现。

---

### 1.3 LLM/OCR 长时间同步等待的稳定性风险

**事实：** 当前 OCR 路径通过 Java `ProcessBuilder` 调用本地 Python 脚本；LLM 路径也是 Java 先启动本地 Python 脚本，再由 Python 脚本发起 OpenAI 兼容 HTTP 请求。部分调用使用 `readAllBytes()` 和 `waitFor()` 同步等待结果，缺少任务状态与取消机制。

**补充说明：** 大模型调用和 OCR 处理本身耗时较长，简单设置较短 timeout 容易把“尚未处理完”的正常任务误判为失败。因此这里不应简单理解为“必须加短超时”。

**影响：**

- 如果 Python OCR/LLM 进程真的卡死，Java 请求线程可能长期阻塞。
- 并发上传时可能造成后端线程耗尽。
- 前端无法知道当前处理到哪一步，也无法主动取消长耗时任务。

**已确定的改造方案：**

- 不设置过短固定超时；OCR 和 LLM 的耗时本身可能正常较长，短超时会把未完成任务误判为失败。
- 复用并扩展 `ocr_log_dim`，使其从“文件进入 `archive/failed` 后由扫描补写的历史日志”变为“文件处理任务日志”；直接使用现有 `log_id` 作为单文件任务标识，无需单独新建任务表。
- 仅新增两个字段：`message` 和 `updated_at`。`message` 只保存正常处理阶段，例如“等待 OCR 启动”“OCR：处理第 2/5 页”“LLM：等待模型响应”“字段校正中”“数据入库中”；`updated_at` 在每次状态或阶段变化时更新。现有 `error_message` 只保存失败原因，不能混用为阶段说明。
- 复用现有 `file_name` 记录写入 `storage/temp` 后生成的实际唯一文件名，作为业务更新和扫描兜底的匹配键；不需要新增 `storage_path`、`original_file_name` 或 `batch_id`。
- 上传文件写入 `storage/temp` 后立即插入一条 `recognize_status=processing` 的日志。OCR/LLM 业务代码在关键节点更新同一条记录的 `message`，不展示伪精确的百分比。
- 前端复用现有“OCR 识别进程”表格：状态列显示 `recognize_status`；新增“处理信息”列，处理中显示 `message`，失败时显示 `error_message`，取消时显示“用户已取消”。`updated_at` 可先不展示，仅用于判断任务是否长期无更新。
- 文件进入 `archive` 或 `failed` 后，业务代码更新同一条日志为 `success`、`warning` 或 `failed`；用户主动取消使用 `cancelled` 状态，不新增取消字段。OCR 可由 Java 通过内存中 `Map<log_id, Process>` 终止本地 Python 进程树；LLM 可中断本地 HTTP 等待，但不保证远端模型已停止处理或停止计费。
- 目录扫描保留，但降级为兜底：补录历史文件、修复异常中断后缺失的终态日志；正常上传流程不再依赖扫描决定日志。扫描逻辑必须识别已有实时日志并跳过，避免重复插入。
- 服务启动时，应把长期停留在 `processing` 且无活动进程的任务更新为 `failed`，并在 `error_message` 中说明“服务重启导致任务中断”，避免页面永久显示“处理中”。
- 论文中若描述系统稳定性，应补充“长耗时 OCR/LLM 任务的阶段日志、人工取消与异常恢复机制”，而不是简单写 timeout。

---

### 1.4 CORS 与 Session 策略需要重新审视

**事实：** 安全配置中存在较宽泛的跨域允许规则，并且 Session 策略与 JWT 无状态设计存在不一致风险。

**影响：**

- 如果 `allowCredentials` 与宽泛 origin 同时使用，部署到公网时风险较高。
- `SessionCreationPolicy.ALWAYS` 与 JWT 架构目标冲突。

**建议：**

- 生产环境限制明确域名白名单。
- 如果采用 JWT，无特殊必要应改为无状态 Session 策略。
- `REDIS.md` 中验证码迁移 Redis 后，也应同步移除验证码对 `HttpSession` 的依赖。

---

## 2. Redis 方案能补上的不足

`REDIS.md` 主要解决的是“临时状态与缓存”问题，而不是算法、安全和论文实验问题。

可解决或缓解：

- 验证码依赖 `HttpSession`，与 JWT 无状态设计不一致。
- 登录/验证码限流使用 JVM 内存，无法支持多实例。
- Dashboard 统计高频重复聚合查询。
- 维度表下拉/列表接口反复整表查询。

不能解决：

- 预测脚本依赖不兼容。
- 权限校验不足。
- OCR/LLM 长耗时任务的进度展示与人工取消。
- 论文实验数据缺乏真实支撑。

已补充到 `REDIS.md` 的关键点：

- `StorageService.moveArchiveFile` 会影响 Dashboard 的 `totalFiles`，需要 evict dashboard。
- `StorageService.failedFile` 可能影响后续日志同步和处理状态。
- `QualityScoreService.scoreFile` 影响 `avgQuality`。
- `fuzzyLookupDegree` 虽然不需要 evict degree 维度缓存，但会影响 `degreeDistribution`，仍需 evict dashboard。
- `todayUploads` 依赖 `ocr_log_dim`，成功无 warning 时是否立即写日志需要统一业务口径。

---

## 3. 论文与实际项目不一致或表述过强的问题

### 3.1 实验数据仍是占位或缺少可复现实证

**事实：** 论文中存在“第 5 章实验数据为占位数据，需根据实际实验结果替换”的说明，同时正文中又给出了 F1、平均耗时、成本下降、RAG 准确率等具体数值。

**问题：**

- 如果这些数值没有真实实验记录支撑，论文结论不能作为最终定稿。
- 目前应视为“待验证数据”，不能作为事实结论。

**建议：**

- 建立真实测试集。
- 固定测试环境、依赖版本、模型版本、硬件配置。
- 给出实验样本量、评价指标定义、统计方式。
- 删除或标注所有未验证数值。

---

### 3.2 “ARIMA + XGBoost”预测表述需要降级或补证据

**事实：** 论文描述了 ARIMA + XGBoost 预测与报告生成能力，但当前预测脚本存在 ARIMA 调用兼容性问题。

**问题：**

- 如果实际运行失败，则不能支撑论文中的预测实验。
- 即使修复运行，也需要证明 XGBoost 与 ARIMA 的组合逻辑、对比基线和误差指标。

**建议：**

- 先修复脚本。
- 补充 MAE、RMSE、MAPE 等指标。
- 给出单独 ARIMA、单独 XGBoost、组合模型的对比。

---

### 3.3 RAG top-k 表述与实现不完全一致

**事实：**

- 聊天上下文注入路径中，知识库检索实际使用 `top_k=3`。
- 检索器默认值存在 `top_k=5`。

**问题：**

- 如果论文统一写 RAG top-5，会与具体聊天实现不完全一致。

**建议：**

- 论文改成：“检索器默认支持 top-5，当前问答上下文注入使用 top-3”。
- 或统一代码参数后再写论文。

---

### 3.4 数据库工具数量表述不一致

**事实：** 论文中提到 19 个数据库查询工具，但代码工具列表实际包含更多工具，并包含 `web_search`、`web_fetch` 等非数据库工具。

**问题：**

- “19 个数据库查询工具”的表述不精确。
- 如果把 web 工具也算入，类别又不一致。

**建议：**

- 重新统计工具数量。
- 区分“数据库查询工具”“知识库检索工具”“联网工具”。
- 论文中给出分类表，而不是笼统写总数。

---

### 3.5 Docker 部署描述与实际 Python 进程方式不完全一致

**事实：** 论文中描述 Python FastAPI 为独立服务进程；实际项目中存在由 Spring Boot 后端管理/启动 Python 进程的逻辑。

**问题：**

- 部署架构图和实际运行形态不一致。
- 这会影响论文对系统模块边界、容器职责和可维护性的描述。

**建议：**

- 如果实际部署是后端容器内启动 Python，则论文应如实描述为“Java 后端编排 Python 服务/脚本进程”。
- 如果希望论文描述为独立 Python 服务，则需要调整 docker-compose 和服务边界。

---

### 3.6 Word 报告生成能力表述需要核实

**事实：** 论文描述支持 Word 报告与 A3 海报等报告输出；代码中可见报告数据接口和前端打印/PDF 方向实现，但未确认存在完整 Word 文档生成接口。

**问题：**

- 如果没有 `.docx` 生成能力，论文中“支持 Word 报告”属于实现不符。

**建议：**

- 要么实现后端 Word 导出。
- 要么论文改成“支持报告数据生成与前端可视化展示/打印导出”。

---

### 3.7 pgvector 已使用，但缺少向量索引说明

**事实：** 项目 schema 中使用了 pgvector，并设置 1024 维 embedding；检索 SQL 使用向量距离排序。但未看到 ANN/vector index 的明确创建。

**问题：**

- 数据量小时可用；数据量上来后，向量检索性能可能下降。
- 论文如果强调高效 RAG 检索，需要补充索引方案或实验数据。

**建议：**

- 增加 HNSW 或 IVFFLAT 索引，并说明参数。
- 在论文中补充不同知识库规模下的检索耗时。

---

## 4. 论文中基本符合实现的部分

以下内容不应再列为“不符”：

### 4.1 四级元数据映射

项目中存在元数据映射服务，包含表头匹配、标准字段映射和编辑距离纠错逻辑。论文中关于四级映射的描述整体有代码支撑。

### 4.2 贪心字段分配算法

项目中确实存在 `FieldCorrectionService.autoCorrectFields(...)`，并在图片和 PDF 处理流程中调用。

更准确的论文表述应为：

> 在元数据表头映射前，系统基于字段值特征对候选字段进行评分，并采用启发式贪心策略完成字段归位。

需要避免写成“全局最优分配算法”，因为当前实现是局部贪心，不是匈牙利算法或严格全局最优匹配。

### 4.3 PPStructure / LLM 双路径处理

项目中存在传统 OCR/结构化解析路径，也存在 LLM 路径。论文关于双路径处理的总体方向成立。

但“自动低质量判断并分派”的细节需要谨慎：如果代码没有完整实现基于质量阈值自动分派，就应改成“支持手动选择或策略扩展”。

---

## 5. 建议的论文修改方向

### 5.1 降低未经验证的强结论

把以下表达从确定性结论改为实验待验证或实现描述：

- “F1 达到 93.7%”
- “平均处理时间 2.3s”
- “成本下降 67.5%”
- “RAG 准确率 94%”

除非已经有真实实验记录，否则应删除或标注为占位。

### 5.2 增加工程约束说明

建议补充：

- Redis 用于验证码、限流和查询缓存。
- Redis 不承担持久化事实数据。
- Dashboard 缓存需要主动 evict + TTL 兜底。
- OCR/LLM 长耗时任务需要任务化、进度反馈、人工取消、错误捕获和资源释放。

### 5.3 增加复现实验设计

建议至少补充：

- 测试数据来源与样本量。
- 档案类型分布。
- 清晰/模糊/老旧扫描件比例。
- 字段级准确率、记录级准确率、处理耗时。
- PPStructure 与 LLM 路径的对比。
- RAG 检索 top-k、embedding 模型、向量维度、索引方式。
- 预测模型的训练/测试划分和误差指标。

### 5.4 区分“已实现”“方案中”“未来工作”

建议在论文或附录中增加一张实现状态表：

| 模块 | 论文描述 | 代码状态 | 建议处理 |
|---|---|---|---|
| 四级元数据映射 | 已实现 | 基本符合 | 保留 |
| 贪心字段分配 | 已实现 | 基本符合 | 保留，但限定为启发式 |
| Redis 缓存 | 方案中 | 文档存在，代码未实现 | 写成待实现/优化 |
| ARIMA + XGBoost | 已描述 | 需修复并验证 | 补实验或降级表述 |
| Word 报告 | 已描述 | 未确认完整实现 | 实现或删除 |
| RAG top-5 | 已描述 | 实现参数不完全一致 | 统一参数或改论文 |

---

## 6. 优先级建议

### 高优先级

1. 修复预测脚本兼容性。
2. 清理论文中的占位实验数据。
3. 修正权限控制问题。
4. 明确 Redis 只是方案还是已实现。

### 中优先级

1. 梳理 LLM/OCR 长耗时任务的任务化处理方案，避免无限阻塞，但不要用过短 timeout 误杀正常任务。
2. 统一 RAG top-k 口径。
3. 补充 pgvector 索引或性能说明。

### 低优先级

1. 优化论文架构图。
2. 增加工具清单分类表。
3. 补充部署差异说明。
