# 项目代码与论文一致性问题清单

> 范围：基于当前仓库代码、`paper` 中论文内容以及已验证的运行/代码检查结果整理。  
> 目的：区分“项目真实存在的问题”“论文与实现不一致的问题”“论文需要补强的表达与实验问题”。  
> 说明：本文档同时记录“当前实现事实”“论文需同步修改的内容”和“尚待验证的事项”；不能把已编译通过等同于业务运行已验证。

---

## 1. 项目代码层面的已发现问题

### 1.1 预测脚本兼容性已修复，但论文实验仍需重跑

**原问题：** `predict_admission.py` 曾调用：

```python
model.fit(disp=False)
```

当前环境中的 `statsmodels==0.14.6` 的 `ARIMA.fit()` 不支持 `disp` 参数，会抛出：

```text
TypeError: ARIMA.fit() got an unexpected keyword argument 'disp'
```

**当前状态：** 代码已改为 `model.fit()`，兼容性错误已修复并提交；但论文中的预测实验结果不能因此自动视为有效，仍需在固定数据集和依赖版本下重新运行。

**论文需修改：**

- 删除“当前实现无法运行 ARIMA”的旧描述（如果论文中存在）。
- 在实验环境中注明 `statsmodels` 等关键依赖版本。
- 重新给出 ARIMA、XGBoost 及组合方案的真实误差指标；未重跑前，不沿用旧数值。

---

### 1.2 管理类接口权限校验不足

**事实：** 多个管理接口存在新增、修改、删除入口，但未在 Controller 层看到统一的管理员权限校验，例如：

- `MetaDataController`
- `CollegeController`
- `MajorController`
- `ClassController`

相比之下，用户管理接口中存在显式 token/role 判断。

**影响：**

- 如果安全配置没有在全局拦截层限制角色，普通登录用户可能访问高权限写接口。
- 论文若描述系统具备完善权限管理，需要补充实际授权机制或修正表述。

**建议：**

- 在后端增加统一鉴权注解或拦截器，例如 `@PreAuthorize("hasRole('ADMIN')")`。
- 对所有写接口做权限矩阵。
- 论文中避免笼统写“权限控制完善”，除非有可验证实现。

---

### 1.3 OCR/LLM 长耗时任务已任务化，论文需要同步实现

**当前实现事实：**

- 文件保存到 `storage/temp` 后，后端立即在 `ocr_log_dim` 创建 `processing` 记录，并把现有 `log_id` 作为单文件 `taskId` 返回；没有新建任务表。
- 仅新增 `message`、`updated_at` 两个字段。`message` 保存当前处理阶段，`error_message` 仍只保存失败原因。
- 上传请求不再等待完整 OCR/LLM 处理，任务交给后端线程池异步执行。前端“OCR 识别进程”页每 3 秒读取日志，显示 `processing/success/warning/failed/cancelled` 状态和阶段消息。
- LLM 多页处理可显示“处理第 n/m 页”等真实阶段；OCR 当前主要显示启动、识别、字段校正、入库等阶段，不虚构百分比。
- 处理中任务可人工取消。Java 会取消后台任务并终止已登记的本地 Python 进程树；取消 LLM 时只能确认本地等待被中断，不能保证远端模型立即停止计算或停止计费。
- 文件夹扫描仍保留为终态和历史日志兜底，不再是实时进度来源。服务重启后，遗留的 `processing` 任务会标记为失败，避免页面永久停留在处理中。

**论文需要修改的位置：**

1. **第 3.1 节、第 4.3 节和第 4.6 节：** 把“上传请求同步完成识别”改为“上传接收与后台识别分离，每个文件返回任务标识”。
2. **第 4.2 节和附录 A：** 在 `ocr_log_dim` 表结构和数据字典中增加 `message`、`updated_at`，说明 `log_id` 同时承担任务标识；状态枚举补充 `processing` 和 `cancelled`。
3. **第 4.6 节的文件处理流程图/时序说明：** 改为“保存文件 → 创建任务日志 → 返回 taskId → 后台 OCR/LLM → 更新阶段消息 → 写入终态”，并画出人工取消和服务重启恢复分支。
4. **第 3.1 节表示层描述和第 4.4 节：** 补充进程表每 3 秒轮询、阶段消息和取消按钮；不要写进度百分比，因为当前实现提供的是可验证的阶段状态。
5. **第 4.1 节、第 4.5 节、附录 D 和第 6.2 节：** 说明本地 OCR 进程可终止、远端 LLM 不保证真正撤销，以及当前任务/进程注册表位于单个 JVM 内存中，不支持多后端实例之间的任务取消转发。
6. **摘要、第 5.1/5.3/5.7 节和第 6.1 节：** 异步化改善的是上传响应和可观察性，不等于缩短 OCR/LLM 实际计算时间；没有真实压测前，不能据此写“处理耗时下降”或“吞吐量提升”。

**验证边界：** 当前后端已离线编译通过、前端已构建通过；本地开发数据库已具备新增字段，新建 Docker 数据库也会由 `schema.sql` 创建字段，但已有 Docker 数据库卷不会被 `CREATE TABLE IF NOT EXISTS` 自动修改。完整上传、取消、重启恢复仍待人工业务测试。因此论文可写“已实现”，但实验章节必须标注为待验证，不能先写性能结论。

---

### 1.4 CORS 与 Session 策略需要重新审视

**事实：** 安全配置中存在较宽泛的跨域允许规则，并且 Session 策略与 JWT 无状态设计存在不一致风险。

**影响：**

- 如果 `allowCredentials` 与宽泛 origin 同时使用，部署到公网时风险较高。
- `SessionCreationPolicy.ALWAYS` 与 JWT 架构目标冲突。

**建议：**

- 生产环境限制明确域名白名单。
- 如果采用 JWT，无特殊必要应改为无状态 Session 策略。
- `REDIS.md` 中验证码迁移 Redis 后，也应同步移除验证码对 `HttpSession` 的依赖。

---

## 2. Redis 方案能补上的不足

`REDIS.md` 主要解决的是“临时状态与缓存”问题，而不是算法、安全和论文实验问题。

可解决或缓解：

- 验证码依赖 `HttpSession`，与 JWT 无状态设计不一致。
- 登录/验证码限流使用 JVM 内存，无法支持多实例。
- Dashboard 统计高频重复聚合查询。
- 维度表下拉/列表接口反复整表查询。

不能解决：

- 预测脚本依赖不兼容。
- 权限校验不足。
- OCR/LLM 长耗时任务的进度展示与人工取消。
- 论文实验数据缺乏真实支撑。

已补充到 `REDIS.md` 的关键点：

- `StorageService.moveArchiveFile` 会影响 Dashboard 的 `totalFiles`，需要 evict dashboard。
- `StorageService.failedFile` 可能影响后续日志同步和处理状态。
- `QualityScoreService.scoreFile` 影响 `avgQuality`。
- `fuzzyLookupDegree` 虽然不需要 evict degree 维度缓存，但会影响 `degreeDistribution`，仍需 evict dashboard。
- `todayUploads` 依赖 `ocr_log_dim`，成功无 warning 时是否立即写日志需要统一业务口径。

---

## 3. 论文与实际项目不一致或表述过强的问题

### 3.1 实验数据仍是占位或缺少可复现实证

**事实：** 论文中存在“第 5 章实验数据为占位数据，需根据实际实验结果替换”的说明，同时正文中又给出了 F1、平均耗时、成本下降、RAG 准确率等具体数值。

**问题：**

- 如果这些数值没有真实实验记录支撑，论文结论不能作为最终定稿。
- 目前应视为“待验证数据”，不能作为事实结论。

**建议：**

- 建立真实测试集。
- 固定测试环境、依赖版本、模型版本、硬件配置。
- 给出实验样本量、评价指标定义、统计方式。
- 删除或标注所有未验证数值。

---

### 3.2 “ARIMA + XGBoost”预测表述仍需补证据

**事实：** 论文描述了 ARIMA + XGBoost 预测与报告生成能力；ARIMA 调用兼容性错误已经修复，但这只证明代码调用方式已更新，不证明论文中的预测效果数据有效。

**问题：**

- 仍需要证明 XGBoost 与 ARIMA 的组合逻辑、对比基线和误差指标。
- 修复前生成的实验结果应重新核实，不能默认沿用。

**建议：**

- 在固定环境中重跑脚本并保存实验记录。
- 补充 MAE、RMSE、MAPE 等指标。
- 给出单独 ARIMA、单独 XGBoost、组合模型的对比。

---

### 3.3 RAG top-k 表述与实现不完全一致

**事实：**

- 聊天上下文注入路径中，知识库检索实际使用 `top_k=3`。
- 检索器默认值存在 `top_k=5`。

**问题：**

- 如果论文统一写 RAG top-5，会与具体聊天实现不完全一致。

**建议：**

- 论文改成：“检索器默认支持 top-5，当前问答上下文注入使用 top-3”。
- 或统一代码参数后再写论文。

---

### 3.4 数据库工具数量表述不一致

**事实：** 论文中提到 19 个数据库查询工具，但代码工具列表实际包含更多工具，并包含 `web_search`、`web_fetch` 等非数据库工具。

**问题：**

- “19 个数据库查询工具”的表述不精确。
- 如果把 web 工具也算入，类别又不一致。

**建议：**

- 重新统计工具数量。
- 区分“数据库查询工具”“知识库检索工具”“联网工具”。
- 论文中给出分类表，而不是笼统写总数。

---

### 3.5 Docker 部署描述与实际 Python 进程方式不完全一致

**事实：** 论文中描述 Python FastAPI 为独立服务进程；实际 Docker 镜像把 Java、Python 环境和脚本放在同一个后端容器中，由 Spring Boot 启动/管理 Python 进程。镜像特意创建了 `.venv/Scripts/python.exe` 到 Linux Python 的软链接，因此当前代码中的兼容路径本身不是 Docker 故障。

**问题：**

- 部署架构图和实际运行形态不一致。
- 这会影响论文对系统模块边界、容器职责和可维护性的描述。
- 当前异步任务管理器的任务和进程注册表位于后端 JVM 内存；单后端容器可用，但横向扩容为多个后端实例时，取消请求可能无法找到由其他实例执行的任务。
- 新数据库卷会通过 `init.sql → parts/schema.sql` 创建 `message`、`updated_at`；已有 `postgres_data` 卷不会自动重跑初始化脚本，也不会仅因更新 `schema.sql` 获得新字段。

**建议：**

- 如果实际部署是后端容器内启动 Python，则论文应如实描述为“Java 后端编排 Python 服务/脚本进程”。
- 如果希望论文描述为独立 Python 服务，则需要调整 docker-compose 和服务边界。
- 在第 4.1/4.5 节和附录 D 说明新部署以完整 `schema.sql` 初始化；旧部署若需要保留数据，必须先备份再人工补字段，不能直接假设初始化脚本会更新旧表。同时把“当前为单后端实例”写成部署约束；若以后需要多实例，应把任务路由/执行器做成共享机制。

**本次 Docker 检查边界：** `docker compose config --quiet` 已通过，Docker 客户端和服务端版本均为 29.1.3；现有三个项目容器均处于已退出状态。本次按要求没有构建镜像或启动容器，因此尚未验证镜像依赖下载、新库初始化后的启动和容器内 OCR/LLM 实际执行。

---

### 3.6 Word 报告生成能力表述需要核实

**事实：** 论文描述支持 Word 报告与 A3 海报等报告输出；代码中可见报告数据接口和前端打印/PDF 方向实现，但未确认存在完整 Word 文档生成接口。

**问题：**

- 如果没有 `.docx` 生成能力，论文中“支持 Word 报告”属于实现不符。

**建议：**

- 要么实现后端 Word 导出。
- 要么论文改成“支持报告数据生成与前端可视化展示/打印导出”。

---

### 3.7 pgvector 已使用，但缺少向量索引说明

**事实：** 项目 schema 中使用了 pgvector，并设置 1024 维 embedding；检索 SQL 使用向量距离排序。但未看到 ANN/vector index 的明确创建。

**问题：**

- 数据量小时可用；数据量上来后，向量检索性能可能下降。
- 论文如果强调高效 RAG 检索，需要补充索引方案或实验数据。

**建议：**

- 增加 HNSW 或 IVFFLAT 索引，并说明参数。
- 在论文中补充不同知识库规模下的检索耗时。

---

## 4. 论文中基本符合实现的部分

以下内容不应再列为“不符”：

### 4.1 四级元数据映射

项目中存在元数据映射服务，包含表头匹配、标准字段映射和编辑距离纠错逻辑。论文中关于四级映射的描述整体有代码支撑。

### 4.2 贪心字段分配算法

项目中确实存在 `FieldCorrectionService.autoCorrectFields(...)`，并在图片和 PDF 处理流程中调用。

更准确的论文表述应为：

> 在元数据表头映射前，系统基于字段值特征对候选字段进行评分，并采用启发式贪心策略完成字段归位。

需要避免写成“全局最优分配算法”，因为当前实现是局部贪心，不是匈牙利算法或严格全局最优匹配。

### 4.3 PPStructure / LLM 双路径处理

项目中存在传统 OCR/结构化解析路径，也存在 LLM 路径。论文关于双路径处理的总体方向成立。

但“自动低质量判断并分派”的细节需要谨慎：如果代码没有完整实现基于质量阈值自动分派，就应改成“支持手动选择或策略扩展”。

---

## 5. 建议的论文修改方向

### 5.1 降低未经验证的强结论

把以下表达从确定性结论改为实验待验证或实现描述：

- “F1 达到 93.7%”
- “平均处理时间 2.3s”
- “成本下降 67.5%”
- “RAG 准确率 94%”

除非已经有真实实验记录，否则应删除或标注为占位。

### 5.2 增加工程约束说明

建议补充：

- Redis 用于验证码、限流和查询缓存。
- Redis 不承担持久化事实数据。
- Dashboard 缓存需要主动 evict + TTL 兜底。
- OCR/LLM 已采用“单文件 taskId + 数据库阶段日志 + 后台线程池 + 人工取消”，论文应按当前实现描述，而不是继续写同步等待或百分比进度。
- `message` 表示正常处理阶段，`error_message` 表示失败原因；二者不可混用。
- LLM 取消只保证停止本地等待，不保证远端停止；当前内存任务注册表只适用于单后端实例。
- Docker 新数据库由完整 `schema.sql` 初始化；已有数据卷不会自动修改旧表，需要备份后人工补字段或重建数据库。

### 5.3 增加复现实验设计

建议至少补充：

- 测试数据来源与样本量。
- 档案类型分布。
- 清晰/模糊/老旧扫描件比例。
- 字段级准确率、记录级准确率、处理耗时。
- PPStructure 与 LLM 路径的对比。
- RAG 检索 top-k、embedding 模型、向量维度、索引方式。
- 预测模型的训练/测试划分和误差指标。

### 5.4 区分“已实现”“方案中”“未来工作”

建议在论文或附录中增加一张实现状态表：

| 模块 | 论文描述 | 代码状态 | 建议处理 |
|---|---|---|---|
| 四级元数据映射 | 已实现 | 基本符合 | 保留 |
| 贪心字段分配 | 已实现 | 基本符合 | 保留，但限定为启发式 |
| Redis 缓存 | 方案中 | 文档存在，代码未实现 | 写成待实现/优化 |
| OCR/LLM 异步任务 | 应同步补充 | 代码已实现，本地库已有字段，业务流程待验证 | 补任务时序、状态、取消和限制 |
| ARIMA + XGBoost | 已描述 | 兼容性已修复，实验待重跑 | 补真实对比实验 |
| Word 报告 | 已描述 | 未确认完整实现 | 实现或删除 |
| RAG top-5 | 已描述 | 实现参数不完全一致 | 统一参数或改论文 |

---

## 6. 优先级建议

### 高优先级

1. 清理论文中的占位实验数据。
2. 把 OCR/LLM 异步任务、阶段日志、取消语义和单实例限制同步到论文。
3. 明确 Docker 旧数据库卷的保留或重建策略，并完成人工上传、取消、重启恢复测试。
4. 重跑 ARIMA/XGBoost 实验，不能只凭兼容性修复沿用旧结果。
5. 修正权限控制问题。
6. 明确 Redis 只是方案还是已实现。

### 中优先级

1. 统一 RAG top-k 口径。
2. 补充 pgvector 索引或性能说明。
3. 若计划多实例部署，设计共享任务路由与取消机制。

### 低优先级

1. 优化论文架构图。
2. 增加工具清单分类表。
3. 补充部署差异说明。
